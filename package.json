{
  "name": "mcp-meilisearch",
  "version": "1.2.8",
  "description": "Model Context Protocol (MCP) implementation for Meilisearch",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./client": {
      "import": "./dist/client.js",
      "types": "./dist/client.d.ts"
    }
  },
  "workspaces": [
    "demo"
  ],
  "scripts": {
    "demo": "npm run build & npm run dev --workspace=demo",
    "server": "npm run build && node --env-file=.env dist/index.js",
    "prepublishOnly": "rm -rf dist && npm version patch && npm run build",
    "build": "tsc && tsc -p tsconfig.types.json && mkdir -p dist/assets && cp -R src/assets/* dist/assets/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.2",
    "axios": "^1.9.0",
    "openai": "^4.98.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.12.2",
    "npm": ">=10.5.0"
  }
}
