{
  "name": "mcp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "workspaces": [
    "client_cmd",
    "client_web",
    "server"
  ],
  "scripts": {
    "dev:cmd": "lerna run build && lerna run --ignore client_web dev",
    "dev:web": "npm run build --workspace=server && lerna run --ignore client_cmd dev",
    "postinstall": "lerna run install"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1",
    "axios": "^1.9.0",
    "express": "^5.1.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.0",
    "lerna": "^8.2.2",
    "typescript": "^5.8.3"
  }
}
