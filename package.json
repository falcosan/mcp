{
  "name": "mcp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "lerna run build",
    "dev": "npm run build && lerna run dev",
    "client": "npm run build --workspace=client && npm run dev --workspace=client",
    "server": "npm run build --workspace=server && npm run dev --workspace=server",
    "postinstall": "lerna run install"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1",
    "axios": "^1.9.0",
    "express": "^5.1.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.0",
    "lerna": "^8.2.2",
    "typescript": "^5.8.3"
  }
}
