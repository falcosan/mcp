{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "dev": "node --env-file=.env mcp & vite",
    "preview": "npm run build && node --env-file=.env dist/mcp & vite preview"
  },
  "dependencies": {
    "vite": "^7.1.7",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/tsconfig": "^0.8.1"
  }
}
