{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "dev": "node --env-file=.env mcp & vite",
    "preview": "npm run build && node --env-file=.env dist/mcp & vite preview"
  },
  "dependencies": {
    "mcp-meilisearch": "^1.2.4",
    "vite": "^6.3.5",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/tsconfig": "^0.7.0"
  }
}
